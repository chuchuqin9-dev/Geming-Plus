# MOBA 对战模拟器 — 开始使用说明

本模拟器是本地网页应用，无需联网、无需输入命令即可运行。

## 一、如何启动

### Windows

1. 双击 `启动模拟器.bat`
2. 等待浏览器自动打开

### macOS / Linux

1. 双击 `启动模拟器.sh`（如无法双击，可先执行一次：`chmod +x 启动模拟器.sh`）
2. 等待浏览器自动打开

> 若浏览器未自动打开，请手动访问：`http://localhost:5173/`

## 二、使用步骤

1. 双击启动模拟器文件
2. 等待浏览器打开
3. 开始配置英雄和装备
4. 点击开始模拟

## 三、常见问题

- 首次启动时若提示"正在安装依赖"，请稍候片刻（需要 Node.js 环境）。
- 关闭浏览器后如需停止服务，直接关闭弹出的黑色命令窗口即可。
- 如需重新启动，再次点击启动文件即可。

## 四、命令行方式（可选）

也可在项目目录手动运行：

```bash
npm install   # 首次运行
npm run dev   # 启动开发服务器
```

然后访问：`http://localhost:5173/`